<%= simple_form_for(@post) do |f| %>
  <%= f.error_notification %>
  <%= f.input :title %>
  <%= f.input :body %>
  <%# f.association :author, collection: Author.all.collect {|p| [p.username, p.id]} %>
  <%= f.input :published_at %>
  <%= f.button :submit %>
<% end %>

